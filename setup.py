from setuptools import setup, find_packages

setup(name='dictConfigParser',
        version='20091216.1',
        description='Parse INI files into dictionaries.  Because, duh.',
        author='Lars Kellogg-Stedman',
        author_email='lars@seas.harvard.edu',
        packages=['dictConfigParser'],
        )
